# Examples: CLI

**Status: placeholder.**

Planned: runnable invocations of the `spackle` CLI against fixture templates, demonstrating `spackle check`, `spackle fill`, and `spackle generate` flows.

In the meantime, see the repo root `README.md` + the integration tests under `tests/` for concrete usage patterns.
