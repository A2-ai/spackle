# Example: browser preview

**Status: planned.**

Run spackle entirely in the browser for live preview as the user edits slot values. Uses the `web` wasm-pack target and `generateBundle` (no `DiskFs`).

Sketch in [`/docs/wasm/recipes/browser-preview.md`](../../../docs/wasm/recipes/browser-preview.md). Full implementation pending — will include a minimal Vite app with Monaco-editor slot inputs.
