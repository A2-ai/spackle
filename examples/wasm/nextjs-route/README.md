# Example: Next.js API route

**Status: planned.**

Project generation inside a Next.js API route (app router or pages router). Node runtime recommended; Edge runtime will need the `web` or `bundler` target.

Sketch in [`/docs/wasm/recipes/nextjs.md`](../../../docs/wasm/recipes/nextjs.md). Full implementation pending.
