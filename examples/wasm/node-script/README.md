# Example: node-script

**Status: planned.**

Same flow as [`bun-script`](../bun-script/) but running under plain Node.js (22+). Exists to verify the nodejs-target wasm-pack output works without any Bun-specific extensions.

Until this lands, use `bun-script` — it's the same API, and Bun runs the nodejs target fine.
