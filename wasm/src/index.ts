// Public entry point for `@a2-ai/spackle-wasm`. Re-exports the orchestration
// surface defined in `./spackle.ts` so consumers can
// `import { generate, DiskFs } from "@a2-ai/spackle-wasm"`.

export * from "./spackle.ts";
